# Server port (Render/Railway set this automatically — don't change in production)
PORT=5000

# Comma-separated list of allowed frontend domains for CORS
# Example: https://toolshub.online,https://www.toolshub.online
# Use * to allow all origins (fine for testing, restrict in production)
ALLOWED_ORIGINS=*
